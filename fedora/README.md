# Fedora

## Running the fedora install script

```sh
curl -H 'Cache-Control: no-cache, no-store' https://raw.githubusercontent.com/231tr0n/config/main/fedora/fedora.bash > fedora.bash && bash fedora.bash
```
